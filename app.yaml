runtime: python311

env_variables:
  SESSION_SECRET: 'your-production-secret-key-here'

handlers:
- url: /static
  static_dir: static

- url: /.*
  script: auto

automatic_scaling:
  min_instances: 1
  max_instances: 10
